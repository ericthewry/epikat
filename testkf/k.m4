define(Cn,($1 ; $2))
define(And,($1 & $2))

define(Ev,_)

define(Com,-- $1)

define(Aly,aly($1))
define(Bob,bob($1))
define(Boxaly,~aly(~$1))
define(Boxbob,~bob(~$1))

define(Query,-- $2
query $1 = $2)

define(Query2,-- $2
query $1 = $2)

define(World,$1)
